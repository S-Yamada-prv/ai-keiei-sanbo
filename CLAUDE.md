# CLAUDE.md

このファイルは、本プロジェクト(`ai-keiei-sanbo`)で Claude Code を使って作業する際のガイドラインです。

## プロジェクト概要

AI を活用した経営参謀業務のための資料作成プロジェクト。Claude Code を用いて各種資料(レポート、提案書、分析資料など)を作成・更新する。

## セッション言語

- セッション内でのやりとり(会話・説明・確認事項など)は **日本語** で行う。

## ディレクトリ構成

- `materials/` — 生成した資料の格納先。GitHub に登録する対象はこの配下のファイルのみを基本とする。
- 上記以外の作業用ファイル・一時ファイル・ローカル設定などは `.gitignore` により除外する。

新しい種類の資料を追加する場合も、成果物は原則として `materials/` 配下に置くこと。`materials/` 以外の場所に成果物を置く必要がある場合は、`.gitignore` の許可リストを更新すること。

## Git 運用ルール

- 本プロジェクトのリモートリポジトリ: https://github.com/S-Yamada-prv/ai-keiei-sanbo
- **資料を作成・変更するたびに、GitHub へプッシュする。** 作業を区切りごとに放置せず、都度コミット・プッシュする。
- GitHub に登録(コミット)するファイルは、**生成した資料に関連するファイルのみ** とする。
  - それ以外のファイル(作業用一時ファイル、ローカル環境固有のファイル、ソースデータの下書きなど)は `.gitignore` に記載して除外する。
  - `.gitignore` は「基本除外・資料関連のみ許可」の方式(allowlist)を採用している。新しいファイル種別や資料フォルダを追加する場合は、`.gitignore` の許可設定(`!` で始まる行)も合わせて更新すること。
- コミットメッセージは変更内容が分かるよう簡潔に日本語で記載する。
- 破壊的な Git 操作(force push、reset --hard など)は、ユーザーの明示的な許可なく実行しない。

## 作業の進め方

- 資料の作成・修正が完了したら、都度以下を実施する。
  1. `git status` で差分を確認する。
  2. 資料関連ファイルをステージングする。
  3. 変更内容が分かるコミットメッセージでコミットする。
  4. `git push` でリモート(GitHub)へ反映する。
